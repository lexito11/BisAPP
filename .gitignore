# Dependencies (basura, no commitear)
node_modules/
frontend/node_modules/

# Next.js build y cache (basura, no commitear)
frontend/.next/
**/.next/

# Env local (secretos)
.env
.env.local
.env.*.local

# IDE / OS
.DS_Store
*.log
nul

# Supabase
supabase/.branches
supabase/.temp
